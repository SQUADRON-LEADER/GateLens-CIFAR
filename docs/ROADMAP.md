# Roadmap

## Near-term

- Add automatic CIFAR download fallback in Streamlit app.
- Add confusion matrix and per-class accuracy report.
- Add command-line option for early stopping.

## Mid-term

- Compare MLP pruning with lightweight CNN pruning.
- Add mixed precision training option.
- Add richer experiment logging format (CSV/JSON).

## Long-term

- Introduce hard-threshold post-training compression.
- Export compact model variants for deployment benchmarks.
- Add reproducible benchmark suite with fixed seeds and run scripts.
